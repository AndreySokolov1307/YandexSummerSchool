import Foundation

enum Constants {
    enum Strings {
        static let addItemMessage = "User added item"
        static let deleteItemMessage = "User deleted item"
        static let loadItemsMessage = "Items loaded"
        static let urlCreatedMessage = "URL created"
    }
}
